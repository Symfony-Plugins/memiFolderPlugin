fillin:
  enabled: true
validators:
  miStringValidator:
    class: sfStringValidator
    param:
      min: 2
      min_error: Este campo es demasiado corto (minimo 2 caracteres) 
      max: 150
      max_error: Este campo es demasiado largo (maximo 20 caracteres)
fields:
  permiso{nombre_permiso}:
   required: 
     msg: El nombre del permiso no puede dejarse vacio
   miStringValidator:
  permiso{descripcionper}:
   required: 
     msg: La descripción del permiso no puede dejarse vacio, comenta aunque un poco
   miStringValidator:

  